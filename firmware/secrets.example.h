#pragma once

// Copiar como secrets.h SOLO en la máquina de desarrollo.
// secrets.h está ignorado por Git y nunca debe subirse.

#define WIFI_SSID "REPLACE_WIFI_SSID"
#define WIFI_PASSWORD "REPLACE_WIFI_PASSWORD"

#define FIREBASE_API_KEY "REPLACE_FIREBASE_WEB_API_KEY"
#define FIREBASE_DATABASE_URL "https://biosinergia-modulo-portatil-default-rtdb.firebaseio.com"

// Identidad técnica exclusiva del ESP32. No reutilizar una cuenta humana.
#define FIREBASE_DEVICE_EMAIL "REPLACE_DEVICE_EMAIL"
#define FIREBASE_DEVICE_PASSWORD "REPLACE_DEVICE_PASSWORD"

#define BIOSINERGIA_DEVICE_ID "biosinergia_001"
